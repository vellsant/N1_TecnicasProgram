#include <iostream>
using namespace std;

struct ratinho {
    int posX;
    int posY;
};

int localizaRatinho(){
    if(mapa[x][y] == 'P'){
        ratinho.posX = x;
        ratinho.pozY = y;
    }
}
